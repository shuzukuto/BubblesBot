# Workspace Rules

1. **Always Verify Builds**: Whenever modifying the source code, ALWAYS instruct the user to run `dotnet build` or ask the user to restart `run.bat` (which will trigger a build) to ensure that the changes compile correctly and function as intended. Do not leave code changes untested.
